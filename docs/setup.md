# meeting-room - Setup Guide

> Generated by create-workspace.ps1 v3 on 2026-04-08 12:23:10

## Quick Start

```powershell
cd D:\AI-DEV\workspaces\meeting-room
.\scripts\start.ps1
```

## 접속 URL

| Service | URL |
|---------|-----|
| Web     | http://192.168.0.200:3006 |
| API     | http://192.168.0.200:4006/health |
| Admin   | http://192.168.0.200:8086 |


## 사전 요구사항

- Docker Desktop + WSL2
- `ai-dev-network` 존재 (`docker network create ai-dev-network`)

## 스택 정보

- Web:   html-static (port 3006)
- API:   node-express (port 4006)
- Admin: html-static (port 8086)
- DB:    PostgreSQL (port 5438)

## 개발 워크플로우

```powershell
# 시작
.\scripts\start.ps1

# 상태 확인 (컨테이너 + health check)
.\scripts\status.ps1

# 로그 확인
.\scripts\logs.ps1

# 코드 변경 후 재빌드
.\scripts\rebuild.ps1

# 중지
.\scripts\stop.ps1

# DB 초기화
.\scripts\reset-db.ps1
```

## 환경 변수 (.env)

`.env` 파일은 workspace 생성 시 자동 생성됩니다. 수정 시 `rebuild.ps1`을 실행하세요.

주요 변수:
- `WEB_PORT=3006`, `API_PORT=4006`, `ADMIN_PORT=8086`
- `POSTGRES_PORT=5438`, `POSTGRES_DB=meeting-room_db`
- `JWT_SECRET` — 자동 생성 (변경 금지)
